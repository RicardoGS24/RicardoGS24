#include <vector>
#include <map>
#include <set>
#include <algorithm>
#include <iostream>
#include <queue>
#include <stack>
#include <cmath>
#include <string>
#include <sstream>
#include <unordered_map>
#include <iomanip>
using namespace std;

int main()
{
    int t; 
    cin >> t;
    vector<int> results;

    for(int i=0;i<t;i++){
        int n;
        cin >> n;
        vector<int> v;
        for(int k=0;k<n;k++){
            int N;
            cin >> N;
            v.push_back(N);
        }

        if ()
        sort(v.begin(),v.end());
        int sum = v[0]+v[i];


    }
    
    return 0;
}
